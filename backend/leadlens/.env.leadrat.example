# LeadratAdapter configuration (IMPLEMENTATION_PLAN.md G.7).
#
# Created for real in Phase 10 item 6 - there is nothing to put here while the adapter is a
# stub. Leadrat cannot add a new aggregation endpoint (Appendix 2 Q4), so the adapter calls
# its existing per-module endpoints and assembles the result itself.

leadlens.crm.leadrat.base-url=
leadlens.crm.leadrat.api-key=
